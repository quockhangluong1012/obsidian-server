# Base URL for the API server.
# Leave empty in development to use the Vite dev server proxy (see vite.config.ts).
# Set to the deployed API's origin (e.g. https://api.example.com) for other environments.
VITE_API_BASE_URL=
